<!-- markdownlint-disable MD033 -->
<!-- markdownlint-disable MD024 -->

# serde.zlib

`local zlib = require("@std/serde/zlib")`

Encode/decode with the zlib compression algorithm provided by the flake2 crate.

---

### zlib.compress

<h4>

```luau
function zlib.compress(data: string | buffer, level: "Default" | "Fast" | "Best"?) -> buffer,
```

</h4>

Encodes (or compresses) the input data with zlib; "Fast" optimizes for compression speed
whereas "Best" optimizes for compressed size.

---

### zlib.decompress

<h4>

```luau
function zlib.decompress(compressed: buffer) -> buffer,
```

</h4>

---

Autogenerated from [std/serde/zlib.luau](/.seal/typedefs/std/serde/zlib.luau).

*seal* is best experienced with inline, in-editor documentation. Please see the linked typedefs file if this documentation is confusing, too verbose, or inaccurate.
